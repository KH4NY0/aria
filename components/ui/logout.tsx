import {Button} from "@/components/ui/button";

const Logout = () => {
    return (
        <div>
            <form>
                <Button type="submit" variant={"ghost"}>
                    Logout
                </Button>
            </form>
        </div>
    );
}